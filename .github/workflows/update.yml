      - name: Echo GitHub token (safe test)
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          if [[ -z "${GH_TOKEN}" ]]; then
            echo "❌ GH_TOKEN is EMPTY"
            exit 1
          else
            echo "✅ GH_TOKEN is loaded"
          fi
